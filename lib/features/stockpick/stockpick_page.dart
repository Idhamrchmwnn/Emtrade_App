import 'package:flutter/material.dart';

class StockpickPage extends StatelessWidget {
  const StockpickPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Stock Pick"),
      ),
    );
  }
}
